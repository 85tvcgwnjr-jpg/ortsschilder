bin/
.project
.settings
.externalToolBuilders/
.idea/
*.prg
*.iq

# Private keys — never commit
*.der
*.pem
*.key

# Mac + editor noise
.DS_Store
*.swp

# Claude config — alles ignorieren außer RULES.md
.claude/*
!.claude/RULES.md

# Large skill framework (Claude tooling, not app code)
.agents/
source/
resources/
monkey.jungle
manifest.xml

# Edge Function — deployed directly via Supabase dashboard, not via git
supabase/
